<?xml version="1.0" encoding="utf-8"?>
<!--
  Tsunagi talks to a self-hosted server over HTTPS. Cleartext is permitted only
  for the loopback addresses used when running the backend on a development
  machine, so a misconfigured production URL fails loudly instead of silently
  sending SMS contents in the clear.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- Host machine as seen from the Android emulator. -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
